<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onps.dao.DepartmentDAO">
    <resultMap id="departmentList" type="com.onps.model.po.DepartmentPO">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
    </resultMap>
    <select id="getDepartmentList" resultMap="departmentList">
        select * from department
    </select>
    <select id="getDepartment" resultMap="departmentList">
        select * from department
        where id=#{id}
    </select>
    <insert id="insertDepartment">
        insert into department(name)
        values(#{name})
    </insert>
    <delete id="deleteDepartment">
        delete from department
        where id = #{id}
    </delete>
</mapper>